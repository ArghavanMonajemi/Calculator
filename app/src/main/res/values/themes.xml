<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/day_primary</item>
        <item name="colorPrimaryVariant">@color/variant</item>
        <item name="colorOnPrimary">@color/night_primary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Button">
        <item name="android:textSize">@dimen/ButtonTextSize</item>
        <item name="android:padding">@dimen/ButtonPadding</item>
        <item name="android:layout_margin">@dimen/ButtonMargin</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="DayNumberButton" parent="Button">
        <item name="backgroundTint">@color/day_numbers_background</item>
        <item name="android:textColor">@color/night_primary</item>
    </style>

    <style name="OperatorsButton" parent="Button">
        <item name="backgroundTint">@color/variant</item>
        <item name="android:textColor">@color/day_primary</item>
    </style>

    <style name="ClearButton" parent="Button">
        <item name="backgroundTint">@color/clear_btn_background</item>
        <item name="android:textColor">@color/day_primary</item>
    </style>

    <style name="DayResultTextView">
        <item name="android:textColor">@color/night_primary</item>
        <item name="android:textSize">@dimen/ResultTextSize</item>
    </style>

    <style name="HistoryTextView">
        <item name="android:textColor">@color/clear_btn_background</item>
        <item name="android:textSize">@dimen/HistoryTextSize</item>
    </style>
</resources>