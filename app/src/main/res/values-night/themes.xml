<resources>
    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/night_primary</item>
        <item name="colorPrimaryVariant">@color/variant</item>
        <item name="colorOnPrimary">@color/day_primary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!--views custom style-->
        <item name="android:textViewStyle">@style/NightResultTextView</item>
    </style>

    <style name="NightResultTextView">
        <item name="android:textColor">@color/day_primary</item>
        <item name="android:textSize">@dimen/ResultTextSize</item>
        <item name="android:layout_margin">@dimen/ButtonMargin</item>
    </style>
</resources>